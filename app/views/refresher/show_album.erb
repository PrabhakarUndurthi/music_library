<html>
	<head>
		<title></title>
	</head>
	<body>
			<% if @album != nil -%>
				ID: <%= @album.id %><br />
				Title: <%= @album.title %><br />
				Artist: <%= @album.artist %><br />
				Genre: <%= @album.genre %><br />
			<% else -%>
				No record found.
			<% end -%>
			<%= link_to('Back', :action => 'list') %> | 
			<%= link_to('Edit', :action => 'edit', :id => @album.id)%> | 
			<%= link_to('Destroy', {:action => 'destroy', :id => @album.id}, :confirm => 'Are you sure?')%>
	</body>
</html>
